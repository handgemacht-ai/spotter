---
---

<section class="features" id="features">
  <div class="features-header">
    <h2 class="section-label">What It Does</h2>
    <p class="features-title">Full context for every agent session.</p>
  </div>

  <div class="features-list">
    <!-- 1. Transcript + Terminal Timeline -->
    <div class="feature-row">
      <div class="feature-text">
        <div class="feature-heading">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" width="24" height="24"><rect x="2" y="3" width="20" height="14" rx="2"/><path d="M6 8l4 3-4 3"/><line x1="12" y1="14" x2="18" y2="14"/></svg>
          <h3>Transcript + Terminal Timeline</h3>
        </div>
        <p>Review message-by-message transcript and xterm output together, including tool/hook outcomes, timestamps, deltas, and full scrollback context.</p>
      </div>
      <div class="feature-screenshot">
        <figure class="feature-shot">
          <img src={`${import.meta.env.BASE_URL}screenshots/session-transcript.png`} alt="Session transcript view with synchronized terminal output and commit panel" loading="lazy" />
          <figcaption>Session transcript with terminal timeline and review panels.</figcaption>
        </figure>
      </div>
    </div>

    <!-- 2. MCP Review Execution (reversed) -->
    <div class="feature-row feature-row--reversed">
      <div class="feature-text">
        <div class="feature-heading">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" width="24" height="24"><path d="M12 20h9"/><path d="M16.5 3.5a2.121 2.121 0 013 3L7 19l-4 1 1-4L16.5 3.5z"/></svg>
          <h3>MCP Review Execution</h3>
        </div>
        <p>Open a review queue in Spotter, then resolve annotations from Claude via Spotter MCP. Resolution notes, status, and counts sync back live.</p>
      </div>
      <div class="feature-screenshot">
        <figure class="feature-shot">
          <img src={`${import.meta.env.BASE_URL}screenshots/reviews.png`} alt="Reviews page with open and resolved annotations and MCP execution instructions" loading="lazy" />
          <figcaption>Reviews queue designed to run through Claude MCP workflows.</figcaption>
        </figure>
      </div>
    </div>

    <!-- 3. Session to Commit Lineage -->
    <div class="feature-row">
      <div class="feature-text">
        <div class="feature-heading">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" width="24" height="24"><circle cx="12" cy="12" r="3"/><line x1="12" y1="3" x2="12" y2="9"/><line x1="12" y1="15" x2="12" y2="21"/></svg>
          <h3>Session &rarr; Commit Lineage</h3>
        </div>
        <p>Trace commits back to sessions with verified links, confidence, and project scoping so you can move from what shipped to how it was produced.</p>
      </div>
      <div class="feature-screenshot">
        <figure class="feature-shot">
          <img src={`${import.meta.env.BASE_URL}screenshots/history.png`} alt="Commit history view with linked sessions and project filter controls" loading="lazy" />
          <figcaption>Commit history with linked sessions and project filters.</figcaption>
        </figure>
      </div>
    </div>

    <!-- 4. Commit + File Drilldowns (reversed) -->
    <div class="feature-row feature-row--reversed">
      <div class="feature-text">
        <div class="feature-heading">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" width="24" height="24"><path d="M8.5 14.5A2.5 2.5 0 0011 12c0-1.38-.5-2-1-3-1.072-2.143-.224-4.054 2-6 .5 2.5 2 4.9 4 6.5 2 1.6 3 3.5 3 5.5a7 7 0 11-14 0c0-1.153.433-2.294 1-3a2.5 2.5 0 002.5 2.5z"/></svg>
          <h3>Commit + File Drilldowns</h3>
        </div>
        <p>Drill into commit detail, changed files, linked sessions, and co-change groups to answer what changed, where, and why risk moved.</p>
      </div>
      <div class="feature-screenshot">
        <figure class="feature-shot">
          <img src={`${import.meta.env.BASE_URL}screenshots/commit-detail.png`} alt="Commit detail page with changed file links, diff view, and linked sessions" loading="lazy" />
          <figcaption>Commit detail: changed files, diffs, linked sessions, and co-change context.</figcaption>
        </figure>
      </div>
    </div>

    <!-- 5. Risk Analytics -->
    <div class="feature-row">
      <div class="feature-text">
        <div class="feature-heading">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" width="24" height="24"><line x1="18" y1="20" x2="18" y2="10"/><line x1="12" y1="20" x2="12" y2="4"/><line x1="6" y1="20" x2="6" y2="14"/></svg>
          <h3>Risk Analytics</h3>
        </div>
        <p>Use heat maps, hotspots, co-change, and file-size metrics to prioritize high-risk review surfaces instead of scanning every file equally.</p>
      </div>
      <div class="feature-screenshot">
        <figure class="feature-shot">
          <img src={`${import.meta.env.BASE_URL}screenshots/file-metrics.png`} alt="File metrics page showing heat map and hotspot tabs" loading="lazy" />
          <figcaption>File metrics and heat scoring for targeted review.</figcaption>
        </figure>
      </div>
    </div>

    <!-- 6. Flows + Specs (reversed) -->
    <div class="feature-row feature-row--reversed">
      <div class="feature-text">
        <div class="feature-heading">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" width="24" height="24"><path d="M16 21v-2a4 4 0 00-4-4H6a4 4 0 00-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M22 21v-2a4 4 0 00-3-3.87"/><path d="M16 3.13a4 4 0 010 7.75"/></svg>
          <h3>Flows + Specs</h3>
        </div>
        <p>Watch live DAG events in <code>/flows</code>, review spec/test timelines in <code>/specs</code>, and jump across sessions/files with global <code>Cmd-K</code> search.</p>
      </div>
      <div class="feature-screenshot">
        <figure class="feature-shot">
          <img src={`${import.meta.env.BASE_URL}screenshots/flows.png`} alt="Flows page showing live nodes and edges across hooks jobs and agents" loading="lazy" />
          <figcaption>Live flow DAG for hooks, jobs, and agent activity.</figcaption>
        </figure>
      </div>
    </div>
  </div>
</section>

<style>
  .features {
    padding: 6rem 0;
    border-top: 1px solid var(--border);
  }

  .features-header {
    margin-bottom: 4rem;
  }

  .section-label {
    font-family: var(--font-mono);
    font-size: 0.8rem;
    font-weight: 500;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--accent);
    margin-bottom: 1rem;
  }

  .features-title {
    font-size: clamp(1.5rem, 3.5vw, 2.25rem);
    font-weight: 700;
    color: #f0f3f8;
  }

  .features-list {
    display: flex;
    flex-direction: column;
    gap: 5rem;
  }

  .feature-row {
    display: grid;
    grid-template-columns: 5fr 7fr;
    gap: 3rem;
    align-items: center;
  }

  .feature-row--reversed .feature-text {
    order: 2;
  }

  .feature-row--reversed .feature-screenshot {
    order: 1;
  }

  .feature-heading {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-bottom: 1rem;
  }

  .feature-heading svg {
    color: var(--accent);
    flex-shrink: 0;
  }

  .feature-heading h3 {
    font-size: 1.15rem;
    font-weight: 700;
    color: #f0f3f8;
  }

  .feature-text p {
    font-size: 1rem;
    line-height: 1.7;
    color: var(--muted);
  }

  .feature-text code {
    font-family: var(--font-mono);
    font-size: 0.85em;
    color: #f0f3f8;
  }

  .feature-shot {
    margin: 0;
    border: 1px solid var(--border);
    border-radius: 12px;
    background: var(--bg-elevated);
    overflow: hidden;
  }

  .feature-shot img {
    display: block;
    width: 100%;
    aspect-ratio: 16 / 10;
    object-fit: cover;
  }

  .feature-shot figcaption {
    border-top: 1px solid var(--border);
    padding: 0.65rem 0.85rem;
    font-size: 0.72rem;
    font-family: var(--font-mono);
    color: var(--muted);
  }

  @media (max-width: 768px) {
    .feature-row {
      grid-template-columns: 1fr;
    }

    .feature-row--reversed .feature-text,
    .feature-row--reversed .feature-screenshot {
      order: unset;
    }
  }
</style>
