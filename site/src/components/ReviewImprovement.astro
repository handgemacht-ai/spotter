---
---

<section class="review-improvement">
  <div class="ri-overlay"></div>

  <div class="ri-inner">
    <div class="ri-header">
      <h2 class="section-label">Review &amp; Improve</h2>
      <p class="ri-title">Turn observations into better agent behavior.</p>
    </div>

    <div class="ri-grid">
      <div class="ri-left">
        <p class="ri-body">
          Reviews are meant to be executed from Claude: open Spotter's queue, run the
          <strong>spotter-review</strong> workflow over MCP, and resolve annotations with notes as you fix code.
        </p>
        <p class="ri-body">
          Start an <strong>improvement session</strong> directly
          from Spotter to turn review findings into durable guardrails &mdash; rules, hooks, skills,
          and tool constraints &mdash; so the next session starts with fewer repeated mistakes.
        </p>
        <div class="ri-callout">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" width="16" height="16"><line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/></svg>
          <span>MCP keeps execution in your coding loop while Spotter keeps the review audit trail.</span>
        </div>
      </div>

      <div class="ri-right">
        <div class="ri-cards">
          <div class="ri-card">
            <div class="ri-card-header">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" width="16" height="16"><rect x="3" y="11" width="18" height="10" rx="2"/><path d="M7 11V7a5 5 0 0110 0v4"/></svg>
              <span>Spotter MCP</span>
            </div>
            <p>Execute review items inside Claude and sync results back to Spotter</p>
          </div>
          <div class="ri-card">
            <div class="ri-card-header">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" width="16" height="16"><path d="M14 2H6a2 2 0 00-2 2v16a2 2 0 002 2h12a2 2 0 002-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/><polyline points="10 9 9 9 8 9"/></svg>
              <span>CLAUDE.md rules</span>
            </div>
            <p>Codify recurring review feedback into persistent agent instructions</p>
          </div>
          <div class="ri-card">
            <div class="ri-card-header">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" width="16" height="16"><polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/></svg>
              <span>Hooks</span>
            </div>
            <p>Automate pre/post checks and enrich session metadata without friction</p>
          </div>
          <div class="ri-card">
            <div class="ri-card-header">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" width="16" height="16"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg>
              <span>Skills</span>
            </div>
            <p>Package repeatable review and refactor playbooks for future sessions</p>
          </div>
        </div>

        <div class="ri-screenshot">
          <figure class="ri-shot">
            <img src="/screenshots/file-detail.png" alt="File detail view with annotations and linked sessions for review follow-up" loading="lazy" />
            <figcaption>File-level review context with annotations, sessions, and commit links.</figcaption>
          </figure>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .review-improvement {
    position: relative;
    padding: 6rem 0;
    border-top: 1px solid var(--border);
  }

  .ri-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(to bottom, transparent, rgba(212, 145, 94, 0.015), transparent);
    pointer-events: none;
  }

  .ri-inner {
    position: relative;
  }

  .ri-header {
    margin-bottom: 3rem;
  }

  .section-label {
    font-family: var(--font-mono);
    font-size: 0.8rem;
    font-weight: 500;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--accent);
    margin-bottom: 1rem;
  }

  .ri-title {
    font-size: clamp(1.5rem, 3.5vw, 2.25rem);
    font-weight: 700;
    color: #f0f3f8;
  }

  .ri-grid {
    display: grid;
    grid-template-columns: 5fr 7fr;
    gap: 3rem;
  }

  .ri-body {
    font-size: 1rem;
    line-height: 1.7;
    color: var(--text);
    margin-bottom: 1.25rem;
  }

  .ri-body strong {
    color: #f0f3f8;
  }

  .ri-callout {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.85rem;
    color: var(--muted);
    margin-top: 1.5rem;
  }

  .ri-callout svg {
    color: var(--accent);
    flex-shrink: 0;
  }

  .ri-cards {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0.75rem;
  }

  .ri-card {
    background: var(--bg-elevated);
    border: 1px solid var(--border);
    border-radius: 8px;
    padding: 1rem;
  }

  .ri-card-header {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-family: var(--font-mono);
    font-size: 0.8rem;
    font-weight: 500;
    color: var(--text);
    margin-bottom: 0.5rem;
  }

  .ri-card-header svg {
    color: var(--accent);
    flex-shrink: 0;
  }

  .ri-card p {
    font-size: 0.78rem;
    line-height: 1.5;
    color: var(--muted);
  }

  .ri-screenshot {
    margin-top: 1.5rem;
  }

  .ri-shot {
    margin: 0;
    border: 1px solid var(--border);
    border-radius: 12px;
    background: var(--bg-elevated);
    overflow: hidden;
  }

  .ri-shot img {
    display: block;
    width: 100%;
    aspect-ratio: 16 / 10;
    object-fit: cover;
  }

  .ri-shot figcaption {
    border-top: 1px solid var(--border);
    padding: 0.65rem 0.85rem;
    font-size: 0.72rem;
    font-family: var(--font-mono);
    color: var(--muted);
  }

  @media (max-width: 768px) {
    .ri-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
